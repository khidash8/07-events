const inputSectn = document.querySelector(".form-input");

//! keybord events
//* 1: keypress- happens only once
// inputSectn.addEventListener("keypress", (e) => console.log("keypressed"));

//* 2: keyup- happens only once
// inputSectn.addEventListener("keyup", (e) => console.log("keyUp"));

//* 3: keydown- happens until realease
// inputSectn.addEventListener("keydown", (e) => console.log("key Down"));

//! event object properties
//* 1: key
// inputSectn.addEventListener("keypress", (e) => console.log(e.key));

//* 2: keyCode
// inputSectn.addEventListener("keypress", (e) => console.log(e.keyCode));

//* 3: code
// inputSectn.addEventListener("keypress", (e) => console.log(e.code));
